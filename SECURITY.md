# Security Policy

## Secrets

Do not commit API keys, local `.env` files, SQLite databases, downloaded GTFS archives, or generated exports. Use `.env.example` as the public template and keep real values in `.env`.

## Reporting Issues

For a portfolio project, open a private issue or contact the maintainer directly if you find a security-sensitive bug.
